c      
c     SUBROUTINE issolid
c      
      subroutine issolid(k,isolid)
        include 'common_geo.inc'
        include 'common.inc'
            if (k.eq.1) then
              isolid = 1
            endif
            if (k.eq.4) then
              isolid = 1
            endif
            if (k.eq.5) then
              isolid = 1
            endif
            if (k.eq.21) then
              isolid = 1
            endif
            if (k.eq.23) then
              isolid = 1
            endif
            if (k.eq.24) then
              isolid = 1
            endif
            if (k.eq.25) then
              isolid = 1
            endif
            if (k.eq.26) then
              isolid = 1
            endif
            if (k.eq.28) then
              isolid = 1
            endif
            if (k.eq.29) then
              isolid = 1
            endif
            if (k.eq.30) then
              isolid = 1
            endif
            if (k.eq.31) then
              isolid = 1
            endif
            if (k.eq.32) then
              isolid = 1
            endif
            if (k.eq.33) then
              isolid = 1
            endif
            if (k.eq.34) then
              isolid = 1
            endif
            if (k.eq.35) then
              isolid = 1
            endif
            if (k.eq.36) then
              isolid = 1
            endif
            if (k.eq.37) then
              isolid = 1
            endif
            if (k.eq.39) then
              isolid = 1
            endif
            if (k.eq.41) then
              isolid = 1
            endif
            if (k.eq.47) then
              isolid = 1
            endif
            if (k.eq.48) then
              isolid = 1
            endif
            if (k.eq.51) then
              isolid = 1
            endif
            if (k.eq.52) then
              isolid = 1
            endif
            if (k.eq.53) then
              isolid = 1
            endif
            if (k.eq.54) then
              isolid = 1
            endif
      end
